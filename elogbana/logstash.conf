input {
 file {
    path => [ "/data/*.log" ]
    type => "apache-access"
    start_position => "beginning"
  }
}

filter {
  if [type] == "apache-access" {
      grok {
        match => { "message" => "%{COMBINEDAPACHELOG}" }
      }
  }
 }

output{
  elasticsearch{
    host => "127.0.0.1"
  }
}

